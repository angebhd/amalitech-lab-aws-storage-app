package com.amalitech.photo;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class PhotoUploaderApplicationTests {

	@Test
	void contextLoads() {
		// Boots the full context against an in-memory H2 database (see
		// src/test/resources/application.yaml) so no live RDS is needed.
	}

}
